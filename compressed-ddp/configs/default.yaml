training: {epochs: 10, batch_size: 64, learning_rate: 0.01, momentum: 0.9, weight_decay: 1.0e-4}
compression: {enabled: true, method: topk, ratio: 0.01, error_feedback: true}
distributed: {backend: gloo, world_size: 1}
logging: {log_dir: runs/, checkpoint_dir: checkpoints/, log_interval: 10}
